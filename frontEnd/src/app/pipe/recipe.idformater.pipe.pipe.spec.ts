import { RecipeIDFormaterPipePipe } from './recipe.idformater.pipe.pipe';

describe('RecipeIDFormaterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeIDFormaterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
