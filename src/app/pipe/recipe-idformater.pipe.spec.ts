import { RecipeidformatterPipe } from './recipe-idformater.pipe';

describe('RecipeIDFormaterPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeidformatterPipe();
    expect(pipe).toBeTruthy();
  });
});
